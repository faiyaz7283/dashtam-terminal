# Dashtam Terminal - CI Environment
# This file is used by GitHub Actions CI pipeline

# Application
APP_NAME=dashtam-terminal
APP_ENV=ci

# Dashtam API (for integration tests - uses mock in CI)
DASHTAM_API_BASE_URL=https://api.dashtam.localhost
